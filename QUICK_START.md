# 🚀 NoirQR - Быстрый Старт

## ⚡ Быстрый Запуск (3 шага)

### 1️⃣ Backend (Express Server)
```bash
cd server
npm start
```
✅ Работает на **http://localhost:3001**

### 2️⃣ Frontend (React + Vite)
```bash
cd frontend
npm run dev
```
✅ Работает на **http://localhost:5173**

### 3️⃣ Откройте браузер
http://localhost:5173

## 🎯 Основные URL

| Страница | URL | Описание |
|----------|-----|---------|
| **Главная** | http://localhost:5173 | Вход / Регистрация |
| **Админ-панель** | http://localhost:5173 | Управление заведениями (после входа) |
| **Меню** | http://localhost:5173/menu/:slug | Общественное меню |

## 👤 Учетные Данные

### Демо-Аккаунт
- Email: `demo@noirqr.com`
- Password: `demo1234`

### Создать новый аккаунт
1. Нажать "Create Account"
2. Ввести email и пароль
3. Автоматический вход после регистрации

## 📋 Основные Возможности

### ✅ Администратор
1. **Создать заведение** - добавить новый ресторан/кафе
2. **Управлять меню** - добавлять/редактировать товары
3. **Получить QR-код** - для печати или рассылки
4. **Просмотреть заказы** - информация о клиентских заказах (в работе)

### 📱 Клиент
1. **Просмотр меню** - через QR-код или ссылку
2. **Добавление в корзину** - выбор товаров и количества
3. **Оформление заказа** - ввод имени и номера телефона

## 🎨 Новые Компоненты

### `Icon.jsx` - Система Иконок
```jsx
<Icon name="cart" size={20} color="#fff" />
```
Доступные иконки: `home`, `admin`, `menu`, `add`, `edit`, `delete`, `qr`, `order`, `cart`, и др.

### `MenuEditor.jsx` - Редактор Меню
Модальное окно для добавления/редактирования товаров:
- Название товара
- Цена
- Описание
- URL фотографии
- Категория
- Preview фото

### Обновленный `AdminPage.jsx`
- Красивая панель управления
- Карточки заведений
- Управление меню для каждого
- Модальное окно QR-кодов

### Обновленный `MenuPage.jsx`
- Групповая сортировка по категориям
- Карточки товаров с фотографиями
- Боковая панель корзины
- Оформление заказа

## 💡 Примеры Товаров

В `server/db.json` уже есть примеры:

```json
{
  "name": "Капучино",
  "price": 150,
  "description": "Классический итальянский кофе со сливочной пеной",
  "imageUrl": "https://images.unsplash.com/...",
  "category": "☕ Напитки"
}
```

## 🔑 API Endpoints

### Заведения (Venues)
```
GET /api/venues                      - Все заведения
GET /api/venue/:slug                 - Заведение по slug
POST /api/admin/venue                - Создать (с токеном)
DELETE /api/admin/venue/:id          - Удалить (с токеном)
```

### Меню (Menu Items)
```
GET /api/venue/:slug/menu            - Меню заведения
POST /api/admin/venue/:id/menu-item  - Добавить товар (с токеном)
PUT /api/admin/menu-item/:itemId     - Обновить товар (с токеном)
DELETE /api/admin/menu-item/:venueId/:itemId - Удалить (с токеном)
```

### QR Коды
```
GET /api/admin/venue/:id/qr          - QR код заведения (с токеном)
```

### Заказы
```
POST /api/order                      - Создать заказ (публичный)
GET /api/admin/orders                - Все заказы (с токеном)
GET /api/admin/venue/:id/orders      - Заказы заведения (с токеном)
```

## 🔐 Админ Токен
```
Header: x-admin-token: test-admin-123
```

## 📦 Структура Товара

```json
{
  "id": 123456789,
  "name": "Название",
  "price": 150,
  "description": "Описание товара",
  "imageUrl": "https://...",
  "category": "☕ Напитки",
  "isAvailable": true,
  "createdAt": "2024-01-24T20:22:23.482Z"
}
```

## 🎨 Цветовая Схема

- **Primary:** `#fff` (белый)
- **Background:** `#0f0f0f`, `#111`, `#1a1a1a` (темный)
- **Accent:** `#8b5cf6` (фиолетовый)
- **Secondary:** `#ec4899` (розовый)
- **Border:** `#333`

## ⌨️ Горячие Клавиши (При необходимости)

- `Esc` - закрыть модальное окно
- `Enter` - сохранить форму

## 🐛 Решение Проблем

### API не отвечает?
1. Убедитесь, что backend запущен на порту 3001
2. Проверьте файл `.env` в frontend (переменная `VITE_API_URL`)
3. Перезагрузите страницу

### Стили не применяются?
1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Перезагрузите dev сервер `npm run dev`

### QR код не генерируется?
1. Убедитесь, что заведение сохранено
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что admin токен верный

## 📞 Контакты и Поддержка

Для вопросов и предложений об улучшении - обратитесь к разработчику.

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2024  
**Статус:** ✅ Готово к использованию
