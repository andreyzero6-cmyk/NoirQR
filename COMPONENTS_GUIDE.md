# 📖 Примеры Использования Компонентов

## 🎨 Icon Component

### Основное использование
```jsx
import { Icon } from './components/Icon';

// Базовое использование
<Icon name="home" size={24} />

// С цветом
<Icon name="cart" size={20} color="#8b5cf6" />

// В кнопке
<button>
  <Icon name="add" size={16} /> Добавить
</button>
```

### Доступные иконки

#### Навигация
```
home, admin, back, close, menu
```

#### Действия
```
add, edit, delete, save, cancel, refresh, settings
```

#### Товары и Категории
```
coffee, food, salad, dessert, drink, bread, meat, fish, vegetable, fruit, pizza, burger, dish
```

#### Коммерция
```
cart, order, qr, price, payment
```

#### Статус
```
loading, success, error, warning, info
```

#### Профиль и Коммуникация
```
user, users, profile, logout, phone, message, email, telegram
```

#### Общее
```
search, filter, download, upload, share, copy, heart, star, check, expand, collapse
```

---

## 🎛️ AdminPage Component

### Что это делает
Полная админ-панель для управления заведениями и их меню.

### Компоненты внутри
- Header с навигацией
- Форма создания заведения
- Сетка карточек заведений
- Модальное окно для QR-кодов
- Интеграция с MenuEditor

### Автоматические Функции
```jsx
// При клике на "Новое заведение" открывается форма
// При заполнении "Название" автоматически генерируется slug
// Кнопка "Меню" открывает MenuEditor
// Кнопка "QR" показывает QR-код в модальном окне
// Кнопка "Просмотр" открывает публичное меню в новой вкладке
```

### Обработка Ошибок
```jsx
// Ошибки отображаются в красивом блоке
{error && (
  <div style={styles.errorBox}>
    <Icon name="error" size={20} /> {error}
  </div>
)}
```

---

## 📝 MenuEditor Component

### Использование
```jsx
import MenuEditor from './components/MenuEditor';

// В AdminPage при выборе заведения
{selectedVenueForMenu && (
  <MenuEditor
    venueId={selectedVenueForMenu.id}
    onClose={() => setSelectedVenueForMenu(null)}
  />
)}
```

### Состояния Формы
```jsx
// Форма скрыта по умолчанию
// Кнопка "Добавить товар" открывает форму
// При редактировании существующего товара форма заполняется
// После сохранения форма очищается
```

### Валидация
```jsx
// Требуется:
- name (название товара)
- price (цена)

// Опционально:
- description (описание)
- imageUrl (URL фотографии)
- category (категория)
```

### Доступные Категории
```
☕ Напитки
🥗 Салаты
🍔 Основное
🍰 Десерты
📍 Другое
```

---

## 🍽️ MenuPage Component

### Использование
```jsx
import MenuPage from './components/MenuPage';

// В App.jsx роут
<Route path="/menu/:slug" element={<MenuPageWrapper />} />

// MenuPageWrapper
const MenuPageWrapper = () => {
  const { slug } = useParams();
  return <MenuPage slug={slug} />;
};
```

### Функциональность
```jsx
// Автоматически:
1. Загружает меню по slug заведения
2. Группирует товары по категориям
3. Показывает фотографии и описания
4. Управляет корзиной
5. Отправляет заказ на backend
```

### Состояния
```
- loading: Показывает spinner при загрузке
- error: Красивое сообщение об ошибке
- empty cart: "Корзина пуста"
- success: Сообщение после оформления заказа
```

---

## 🏠 HomePage Component

### Структура
```
Header (липкая навигация)
  ├── Logo
  └── Nav Links

Hero Section (главный баннер)
  ├── Заголовок с градиентом
  ├── Подзаголовок
  └── CTA кнопки

Features Section (возможности)
  └── Сетка с 6 карточками

How It Works Section (процесс)
  └── 4 шага в grid

CTA Section (призыв к действию)

Footer (контактная информация)
```

### Компоненты Внутри
```jsx
<FeatureCard icon="icon-name" title="Title" desc="Description" />
<StepCard number="1" icon="icon-name" title="Title" desc="Description" />
```

---

## 🔄 Жизненный Цикл Заказа

### Этап 1: Администратор
```
1. Открывает AdminPage (/admin-secret)
2. Создает новое заведение
3. Нажимает "Меню" на карточке
4. MenuEditor открывается
5. Добавляет товары с фото и описаниями
6. Нажимает "QR" для получения QR-кода
```

### Этап 2: Клиент
```
1. Сканирует QR-код
2. Попадает на MenuPage (/menu/slug)
3. Видит товары, сгруппированные по категориям
4. Добавляет товары в корзину
5. Открывает корзину (боковая панель)
6. Вводит имя и номер телефона
7. Нажимает "Оформить заказ"
```

### Этап 3: Backend
```
1. Получает POST на /api/order
2. Сохраняет заказ в db.json
3. Отправляет уведомление в Telegram (если configured)
4. Возвращает успешный ответ
```

### Этап 4: Результат
```
1. Клиент видит "✅ Заказ принят!"
2. Корзина очищается
3. Администратор может видеть заказ в админ-панели
```

---

## 💻 Примеры API Вызовов

### Получить все заведения
```javascript
import { getVenues } from './api';

const venues = await getVenues();
console.log(venues);
// [{id: 1, name: "Кафе", slug: "kafe", ...}, ...]
```

### Создать заведение
```javascript
import { createVenue } from './api';

const newVenue = await createVenue({
  name: 'Мое Кафе',
  slug: 'moe-kafe',
  description: 'Лучшее кафе в городе',
  telegramId: '123456789'
});
```

### Добавить товар
```javascript
import { createMenuItem } from './api';

const newItem = await createMenuItem(venueId, {
  name: 'Капучино',
  price: 150,
  description: 'Классический кофе',
  imageUrl: 'https://...',
  category: '☕ Напитки'
});
```

### Получить меню
```javascript
import { getMenuItems } from './api';

const menu = await getMenuItems('moe-kafe');
console.log(menu);
// {venue: {...}, items: [...]}
```

### Оформить заказ
```javascript
// Непосредственно через fetch
const response = await fetch('http://localhost:3001/api/order', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'Иван',
    phone: '+79991234567',
    items: [
      { id: 1, name: 'Капучино', price: 150, quantity: 2 }
    ],
    total: 300
  })
});
```

---

## 🎨 Стили и Кастомизация

### Использованные переменные
```javascript
const styles = {
  container: {
    background: 'linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%)',
    color: '#fff',
  },
  highlight: {
    background: 'linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%)',
    WebkitBackgroundClip: 'text',
    WebkitTextFillColor: 'transparent',
    backgroundClip: 'text',
  },
  button: {
    backgroundColor: '#8b5cf6',
    color: '#fff',
    border: 'none',
    borderRadius: '8px',
    fontWeight: '600',
    cursor: 'pointer',
    transition: 'all 0.3s',
  }
};
```

### Как изменить цвета
```javascript
// В styles объекте компонента

// Основной цвет фона
background: 'linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%)',
             // измените #0f0f0f и #1a1a1a

// Акцентный цвет
backgroundColor: '#8b5cf6',  // фиолетовый
backgroundColor: '#ec4899',  // розовый
                 // Измените на нужный hex-код
```

---

## 📱 Responsive Дизайн

Все компоненты адаптивны, но можно добавить дополнительные медиа-запросы:

```javascript
// В глобальных стилях
const style = document.createElement('style');
style.textContent = `
  @media (max-width: 768px) {
    .container {
      padding: 1rem !important;
    }
    h1 {
      font-size: 1.5rem !important;
    }
  }
`;
document.head.appendChild(style);
```

---

## 🚀 Развертывание

### Production Build Frontend
```bash
cd frontend
npm run build
# Создаст папку dist/ с собранными файлами
```

### Production Build Backend
```bash
cd backend
npm start
# Просто запустить, уже оптимизировано
```

### Деплой на сервер
```bash
# Скопировать frontend/dist на веб-сервер (nginx, apache)
# Запустить backend на Node процессе (PM2, Forever)

# Пример с PM2
pm2 start server/index.js --name "noirqr-api"
pm2 save
pm2 startup
```

---

**Последнее обновление:** Январь 2024
